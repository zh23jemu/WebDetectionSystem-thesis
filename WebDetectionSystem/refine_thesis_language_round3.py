import shutil
from pathlib import Path

from docx import Document


DOC_PATH = Path(
    r"C:\Coding\WebDetectionSystem-thesis\WebDetectionSystem\基于Python的Web木材表面缺陷智能检测系统的设计与实现.docx"
)
BACKUP_PATH = DOC_PATH.with_name(DOC_PATH.stem + "_语言优化第三轮前备份.docx")


def set_para_text(paragraph, text):
    """定点替换段落内容，尽量保持原有版式不变。"""
    for run in paragraph.runs:
        run.text = ""
    if paragraph.runs:
        paragraph.runs[0].text = text
    else:
        paragraph.add_run(text)


def replace_by_contains(doc, mapping):
    """按唯一特征短语替换剩余高风险句子。"""
    replaced = 0
    for para in doc.paragraphs:
        text = para.text.strip()
        if not text:
            continue
        for needle, new_text in mapping.items():
            if needle in text:
                set_para_text(para, new_text)
                replaced += 1
                break
    return replaced


def main():
    shutil.copy2(DOC_PATH, BACKUP_PATH)
    doc = Document(DOC_PATH)

    mapping = {
        "能够满足毕业设计演示场景下的基本操作需求": (
            "从系统使用流程来看，系统操作路径较为清晰。登录后，常用功能入口集中显示在左侧导航区域，用户能够较快进入检测、历史记录、日志报表和销售管理等页面。"
            "检测页面支持本地上传与摄像头采集两种方式，管理员相关配置集中在系统设置页面中，能够满足系统日常使用与功能验证需求。"
        ),
        "该设计有助于保证多用户场景下的数据访问边界和操作安全性": (
            "在删除记录操作中，后端增加了权限控制逻辑。管理员可以查看并管理全量检测数据，普通用户仅能操作本人产生的记录，"
            "该设计能够保证多用户场景下的数据访问边界，并提高系统操作的安全性。"
        ),
        "提升系统结果说明的直观性": (
            "帮助中心页面展示了活节、死节和裂纹三类典型样张，并给出相应的定义说明、外观特征和等级判定依据。"
            "该页面能够辅助用户理解不同缺陷之间的差异，并提高检测结果说明的直观程度。"
        ),
        "使用频率较高的核心业务页面之一": (
            "检测页面是系统核心业务页面之一。系统将本地上传和摄像头采集两种方式整合在同一页面中，并通过选项卡进行切换，以提高检测操作的连续性。"
            "页面同时增加了本地图片预览功能，便于用户在提交检测前确认所选文件。"
        ),
        "获得较为完整的结果反馈": (
            "模型返回的原始类别为英文名称，系统在展示结果前会先将其映射为中文缺陷名称，再结合业务规则给出等级结果。"
            "与此同时，页面不仅展示缺陷类别，还同步显示结果图、置信度和等级判定信息，从而使单次检测过程能够同时输出较为完整的结果信息。"
        ),
        "浏览器端主要用于验证登录、图片上传、摄像头采集、图表显示和销售管理等页面功能": (
            "本次测试所使用的环境与系统日常开发环境保持一致。测试平台为 Windows 操作系统，后端程序运行于本地 Python 虚拟环境中，"
            "Web 框架采用 Flask，数据库采用 SQLite，检测模块调用训练完成的模型权重文件进行推理。浏览器端用于验证登录、图片上传、摄像头采集、图表显示和销售管理等页面功能。"
        ),
        "能够满足毕业设计演示和课程实践的基本要求": (
            "综合测试结果可知，系统已经能够较完整地实现论文前文设计的主要功能。系统能够完成木材表面缺陷图像检测、结果等级判定、检测历史追溯、"
            "日志报表统计、库存数量更新、销售出库和管理员后台管理等业务。整体运行过程较为稳定，能够满足系统功能验证与应用展示的基本要求。"
        ),
        "保持了较强的一致性": (
            "总体而言，本文研究工作建立在已完成开发与调试的木材表面缺陷检测系统基础之上。"
            "论文中涉及的大部分页面、业务流程和数据结构均能够在系统实现中找到对应内容，因此论文分析内容与系统实现情况基本一致。"
        ),
        "在硬件和时间条件允许的情况下": (
            "未来可从三个方面继续开展完善工作。其一，进一步补充数据集并优化模型，以提升系统在复杂木纹、光照变化以及多缺陷同时出现条件下的识别稳定性；"
            "其二，将现有单机运行方式逐步扩展为更适合多人使用的部署结构，以提升系统应用范围；"
            "其三，继续完善业务层功能，例如图片文件清理、权限颗粒度控制、日志审计和更完整的销售统计。"
            "在硬件与时间条件允许的前提下，系统还可进一步扩展至移动端采集或现场摄像设备应用场景。"
        ),
    }

    replaced = replace_by_contains(doc, mapping)
    doc.save(DOC_PATH)
    print(f"done, replaced={replaced}")


if __name__ == "__main__":
    main()
