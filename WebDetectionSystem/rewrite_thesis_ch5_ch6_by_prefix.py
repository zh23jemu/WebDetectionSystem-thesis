import shutil
from pathlib import Path

from docx import Document


DOC_PATH = Path(r"C:\Coding\WebDetectionSystem-thesis\WebDetectionSystem\基于Python的Web木材表面缺陷智能检测系统的设计与实现.docx")
BACKUP_PATH = DOC_PATH.with_name(DOC_PATH.stem + "_第五六章前缀优化前备份.docx")


REWRITE = {
    "登录页面采用双标签页设计，分别对应普通用户和管理员登录入口。": "登录模块虽然实现思路比较直接，但做了一个很实用的细节处理：把普通用户和管理员放在同一个页面中，通过标签页区分入口。用户提交表单后，后端除了校验用户名和密码，还会继续判断账号角色是否和当前入口一致。这样做的目的很明确，就是避免普通账号从管理员入口误登，或者管理员账号从普通入口进入。退出登录时，系统还会顺带补写本次工作日志的退出时间，这一记录会直接影响后面报表中的工作时长统计。",
    "登录成功后，系统默认进入仪表盘页面，向用户展示开始检测、查看日志和销售管理三个核心入口。": "仪表盘页面的作用主要是把系统里最常用的几个入口集中起来，避免用户登录后再逐个菜单查找。当前页面放置了开始检测、查看日志和销售管理三个功能卡片，比较适合日常演示和答辩时快速切换。个人中心部分虽然功能不算多，但保留了密码修改这一项，用户修改成功后系统会要求重新登录，以保证新密码能够立即生效。",
    "系统所有登录后的业务页面均继承 `base.html`。": "在页面组织上，系统采用了较为传统但很稳妥的方式：所有登录后的业务页面都继承 `base.html`。侧边栏、提示消息区域以及图表和图片查看相关的公共资源，统一放在基础模板中处理，其他页面只负责各自的功能内容。这样处理之后，页面风格比较统一，后续新增页面时也不需要重复搭建框架。",
    "这种基于模板继承和条件渲染的实现方式，不仅减少了重复页面代码，也保证了不同角色使用体验的一致性。": "从实现效果看，这种模板继承方式虽然不复杂，但对毕业设计项目非常合适。它既减少了重复代码，也让普通用户和管理员进入系统后保持了相对一致的操作习惯。",
    "检测页面通过选项卡方式将“本地上传”和“摄像头采集”两种检测模式整合到同一界面。": "检测页面是整个系统里交互最频繁的页面之一。当前实现把“本地上传”和“摄像头采集”放在同一个页面中，通过选项卡切换，用户不需要在不同页面之间来回跳转。实际调试时，这种组织方式比拆成两个独立页面更顺手，也更适合课程答辩演示。后来为了让操作更直观，页面还补充了本地图片预览，用户在点击检测前就能先确认自己选中的图片。",
    "后端在接收到文件后，会从系统设置中读取当前置信度阈值和模型文件名。": "后端检测流程的入口在 `/detect` 路由中。系统收到图片后，会先读取当前的置信度阈值和模型配置，再把原图保存到上传目录，然后调用 `process_image` 完成推理。这里有一个很实际的问题：模型并不一定始终固定，因此后台设置中保留了模型切换功能，管理员上传新权重后，后续检测会优先使用该模型。",
    "由于一张图片中可能存在多个检测框，系统会对类别名称进行去重并计算最大置信度": "模型返回的原始类别是英文名称，系统并没有直接照搬到前端，而是先映射成中文缺陷，再结合业务规则给出等级结果。与此同时，页面展示的不只是类别文本，还包括结果图、置信度和判定等级，这样用户在一次操作中就能看到完整信息。",
    "项目将缺陷识别结果与板材等级划分结合起来：": "在当前项目里，等级判定不是附属说明，而是检测流程中的关键一步。系统把无缺陷板材记为 A 级；识别到活节时判为 B 级；识别到裂纹或死节时则直接归入 C 级。判定完成后，后端会同步更新库存表，也就是说，一次检测动作同时触发了业务数据变化，而不只是输出一张结果图。",
    "每次检测完成后，系统会构造 `DetectionHistory` 实体并写入数据库，": "每完成一次检测，系统都会把结果写入 `DetectionHistory` 表。保存的字段包括原始文件名、结果图文件名、缺陷类型、最高置信度、操作用户和等级结果。结果图本身则单独保存在 `static/results` 目录中，前端页面通过静态路径直接访问。这样的处理方式比较清楚：图片和结构化记录分开保存，但页面和报表又都能把它们关联起来。",
    "这种设计使检测结果既能够被系统页面立即消费，也能够在后续历史记录和报表分析中重复使用，是系统实现“可追溯”能力的核心。": "从后续使用来看，这一设计让历史追溯、图片预览和统计报表都建立在同一批检测数据之上，不需要额外再做二次整理。",
    "历史记录页面采用卡片式布局呈现检测结果图、文件名、等级、缺陷类型和检测时间。": "历史记录页面的实现重点在于“可查”和“可看”。页面把结果图、文件名、等级、缺陷类型和检测时间放在同一张卡片中，同时支持按缺陷关键词进行筛选。由于检测记录会不断积累，系统采用分页方式控制单页数量，避免页面一次性堆积太多内容。",
    "对于删除操作，后端会进一步校验当前用户是否拥有该记录的操作权限": "删除记录时，后端还额外做了一层权限判断。管理员可以查看并管理全量数据，普通用户只能操作自己的检测记录。这样的处理虽然简单，但能避免多人演示时出现权限混乱的问题。",
    "日志与报表模块负责将历史检测数据转换为可视化统计结果。": "日志与报表模块并不是单纯做页面美化，它的核心作用是把已经保存下来的检测数据重新组织成便于查看的统计结果。系统会统计今日检测量、缺陷类别占比和近 7 天检测趋势，并把这些结果传给前端图表组件渲染。就当前项目而言，这些统计已经足够支持毕业设计展示。",
    "在数据导出方面，系统利用 pandas 和 openpyxl": "在数据导出方面，系统通过 pandas 和 openpyxl 把检测记录整理成 Excel 文件，再以浏览器下载的方式返回给用户。这个功能在答辩或成果展示时比较实用，因为它能够把系统里的数据直接导出成可提交、可查看的文件。",
    "系统在用户登录时自动写入登录时间，在退出时补充退出时间。": "工作时长统计来源于 `WorkLog` 表。用户每次登录时系统都会新增一条日志记录，退出时再补上离开时间。报表页在统计累计工作时长时，会把已经结束的记录和当前仍处于登录状态的记录一起计算，所以页面上显示的是一个动态变化的结果，而不是固定写死的数字。",
    "该功能虽然实现逻辑较为简单，但非常契合实验教学或质检值班管理场景": "虽然这部分实现逻辑并不复杂，但它让系统不只是“识别了多少张图片”，还能进一步反映“工作持续了多长时间”，这对教学演示来说算是一个比较细的小亮点。",
    "销售页面分为库存概览和订单创建两部分。": "销售页面的实现思路比较直接，但它和检测业务结合得很紧。页面上方先展示 A、B、C 三类库存数量，下方再提供订单录入表单。用户提交销售订单后，系统会先判断当前等级库存是否足够，只有库存满足条件时才允许出库并写入销售记录。这样可以避免销售数据和库存数据脱节。",
    "管理员用户管理页面提供新增和删除用户功能。": "用户管理模块主要面向管理员使用。当前实现支持新增用户和删除用户，新用户创建后密码会先经过哈希再入库，而不是以明文形式保存。虽然这一模块页面并不复杂，但它让系统具备了最基本的账号维护能力。",
    "系统设置页面允许管理员动态调整置信度阈值，并上传新的 `.pt` 模型文件。": "系统设置页主要保留了两个可调项目：检测阈值和模型文件。阈值调整会直接影响后续检测时保留哪些检测框；模型上传则让管理员能够在不改代码的前提下切换新的 `.pt` 权重文件。对毕业设计项目来说，这一页比较直观地体现了系统的可维护性。",
    "在 `__main__` 启动逻辑中，系统自动创建数据库表、默认管理员账号以及三类库存记录": "项目启动时会自动完成一些初始化动作，例如建表、创建默认管理员账号和初始化库存记录。这样做的好处很明显：新环境第一次运行时，不需要再手动补数据库脚本或单独准备初始数据，直接启动就能进入演示流程。",
    "同时，系统会自动创建上传目录与结果目录，保证图片检测流程在空目录环境下也能正常运行": "另外，系统还会自动检查并创建上传目录和结果目录。实际调试时，如果没有这一步，第一次检测很容易因为目录不存在而出错，因此这类初始化逻辑虽然不起眼，但很有必要。",
    "帮助中心页面展示活节、死节和裂纹三类典型缺陷图谱": "帮助中心页面放置了活节、死节和裂纹三类典型样张，同时给出简短定义、外观特征和等级说明。这个页面对系统运行本身不是必须的，但对第一次接触项目的人很有帮助，特别适合在答辩时辅助说明不同缺陷之间的区别。",
    "系统测试的主要目标是验证当前项目各功能模块是否能够按照预期完成业务流程": "系统测试部分的重点并不是追求非常复杂的性能指标，而是确认当前项目的主要功能链路能否稳定跑通。具体包括登录鉴权、图片检测、等级判定、检测历史保存、报表统计、Excel 导出、销售扣库以及管理员设置等内容。换句话说，测试更关注“这个系统能不能完整用起来”，而不是只看某一个模块单独是否成功。",
    "测试环境基于 Windows 开发主机": "本次测试环境基于 Windows 开发主机，程序运行在项目本地 `.venv` 中，后端框架为 Flask，数据库为 SQLite，模型文件使用项目内配置的 `.pt` 权重。浏览器端还需要能够正常访问摄像头权限，这样才能验证拍照检测流程是否可用。",
    "在测试过程中，分别以普通用户和管理员两种身份进入系统": "在测试过程中，分别以普通用户和管理员两种身份进入系统，对核心页面和关键流程进行了逐项操作。测试方式以功能验证为主，同时也顺带检查了一些异常输入场景，例如未选择文件直接检测、角色入口选错、库存不足时继续提交订单等。",
    "除正向测试外，还对空文件上传、角色入口错误、库存不足、无权限删除历史记录等异常场景进行了验证": "除了正常流程外，系统还对若干异常情况进行了验证。例如未选择图片时，检测页面会先提示用户；当用户从错误的角色入口尝试登录时，系统会明确给出身份不匹配提示；库存不足时，销售页面也不会继续写入订单。从当前运行结果看，这些边界情况都能得到比较明确的反馈。",
    "从当前代码实现和功能验证结果看，系统能够完成预定的核心业务流程：": "结合实际运行情况可以看出，系统已经能够完成预期的核心流程：登录与权限控制正常，检测模块能够输出缺陷类型和等级结果，历史记录和报表模块可以完成追溯与统计，销售模块能够和库存联动，管理员也可以进行基础参数配置和模型切换。对毕业设计演示和课程实践而言，这样的功能完整度已经能够支撑论文中的主要论述。",
    "需要指出的是，现阶段系统仍以单机 SQLite 部署为主": "当然，系统仍然存在一些可以继续完善的地方。例如当前删除历史记录时只删除数据库记录，并没有同步删除对应图片文件；数据库仍然采用 SQLite，更适合单机演示；测试部分也主要集中在功能验证，尚未覆盖更严格的并发、异常恢复和长期运行稳定性测试。"
}


def replace_text(para, new_text):
    for run in para.runs:
        run.text = ""
    if para.runs:
        para.runs[0].text = new_text
    else:
        para.add_run(new_text)


def main():
    shutil.copy2(DOC_PATH, BACKUP_PATH)
    doc = Document(DOC_PATH)
    changed = 0
    for para in doc.paragraphs:
        text = para.text.strip()
        for prefix, new_text in REWRITE.items():
            if text.startswith(prefix):
                replace_text(para, new_text)
                changed += 1
                break
    doc.save(DOC_PATH)
    print(changed)


if __name__ == "__main__":
    main()
